# Roadmap

What's worth building next, in three buckets. Items move down the file as they ship — when something ships it leaves this file and lands in [`CHANGELOG.md`](CHANGELOG.md). Things that won't be done at all live in the "Won't do" section so the reasoning isn't lost.

Last reviewed: 2026-06-07 (reconciled against code: KC arc, R1 retrieval loop, R2 SM-2, R3 mastery gates, R10/R11/R12, and the plan-drawer removal all verified shipped — moved to CHANGELOG. Remaining pedagogy work is the practice-testing core, R9 + R13, plus R5–R8).
Annotated 2026-06-17 (transcript-grounded UX/pedagogy review: frequency-prioritised the preamble-friction item; added real-usage breadth evidence to courses-drawer. Findings only — no new items, since the review mostly validated the existing backlog).
Annotated 2026-06-17 (Eduardo flagged the **knowledge-base project as the next major milestone** — enriched the Later → Knowledge base item with the substrate inventory + the siloed-atom structural gap + its convergence with interleaving-v2 and the KC link-graph follow-on. Gated on a scoping grill before any schema/indexer work).

## Now

- **Designated next major milestone: the knowledge-base project** — see the *Knowledge base* item under [Later](#later). NOT build-ready: gated on a scoping grill/brainstorm to pin the shape before any schema or indexer work. Flagged by Eduardo 2026-06-17.

Nothing else urgent.

## Next

Cheap, small, ready when there's an excuse to pull them in.

- **Session preamble friction.** The session-open sequence (retrieval round + pre-testing + pre-read prediction) and session-close free recall feel grinding, especially at the start and end when the learner is either cold or tired. The pedagogical evidence is solid but the rigid always-fire implementation might need tuning: a "light session" mode that skips preamble after N consecutive strong scores, a "skip preamble" affordance, or a timed decay (shorter preamble when sessions are frequent). Brainstorm before designing — this is about UX tuning, not removing the evidence-backed mechanisms.
  > 2026-06-17 transcript review: **highest-ROI UX item by frequency** — the preamble fires on *every* session, whereas the bigger pedagogy gaps (e.g. R5) hit only a handful. Real transcripts (sess 59/60) confirm the preamble's free-recall/retrieval is exactly where learning happens, so the goal is **cadence tuning, not removal** — favour the timed-decay / consecutive-strong-score variants over a blunt skip.
  > 2026-06-17 (Eduardo's call): **session-*open* friction accepted as-is — do not pursue open-sequence tuning.** The always-fire opener is working for him. Any remaining work here is limited to the session-*close* free recall (not separately reviewed).
- **Courses-drawer UX review.** Current courses/sessions management feels poor. Brainstorm pass first — list what's clumsy, what's missing, what should disappear — before touching code. Likely splits into 2–3 small follow-up items. (The right-side plan drawer was already removed 2026-06-07 — left rail is the single plan view; this broader management-UX brainstorm is still open.)
  > 2026-06-17 transcript review: real usage is **broader and more ad-hoc than the "small fixed set" model assumes** — sessions span guitar, verifier-stats, critical-theory, and uncategorised work alongside the core courses. The hardcoded `courseMeta` map + fixed-set framing (CONTEXT.md) under-serves this; the brainstorm should treat agent-created / ad-hoc courses as first-class, not fallback-coloured afterthoughts.
- **Phase 2.6 — migration system.** Inline migrations in `agent/db.go` cover the current schema. The first time the schema needs a non-trivial change, replace them with a numbered-migration runner (something `golang-migrate`-shaped or a tiny in-tree version).
- **Cloudflare Access on top of bearer auth.** Optional second auth layer at the CF edge. Belt-and-suspenders — only worth it if you want zero unauthenticated traffic ever reaching the app.
- **Harden task-UUID preservation across plan rewrites.** Phase 3 anchors a Session to a plan task's UUID via a soft `sessions.task_id` ref (no FK — plan tasks are JSON, not a table; see ADR 0011/the Phase-3 data-model decision). `ToolRewritePlan` → `buildTitleToIDMap` / `inheritOrGenerateIDs` currently matches on the **normalized title** (case-insensitive trim) — so a genuine title change still regenerates the UUID and orphans the Session (it falls into the "Detached" bucket). Reduce that: match on a stable slug rather than the title, and/or have the tutor pass through old IDs on rewrite. Doesn't fully close orphaning (the accepted Phase-3 trade-off is that orphans are *survivable + recoverable*, not impossible) — this just makes it rare. The structural fix (tasks as a DB table + real FK) was considered and rejected for Phase 3 as a multi-day migration touching `claw-cli` and the overnight pipeline.

### Pedagogy backlog (graded by evidence strength)

The encoding front-half (Rules 1–8) and the retrieval back-half core — KC arc, R1 retrieval loop, R2 SM-2 spacing, R3 mastery gates — have all shipped (see CHANGELOG). What's left below is the practice-testing signal-quality work (Practice-testing gap section) plus R5–R8. Items ranked by evidence weight (Roediger & Karpicke 2006; Dunlosky et al. 2013).

- **Knowledge Component arc — deferred follow-ons.** The four core tickets (rename → `knowledge_components` entity → `knowledge_create` capture tool → retrieval-practice loop) shipped by 2026-06-01 (see [ADR 0007](docs/adr/0007-knowledge-component-as-atomic-note.md) + [CONTEXT.md](CONTEXT.md)). What remains is the deferred follow-on work:
  - **HTML capture box.** Spec'd 2026-06-08 (see `specs/queue/2026-06-08-kc-capture-box.md`). Frontend authoring surface where the learner types the note body into a titled box and submits. Waiting for overnight pipeline pickup.
  - **Link graph + multi-source provenance.** The synthesis-note feature: links between Knowledge Components, and provenance promoted to its own table (one atom can derive from several sources). Additive over the entity, no rework — see ADR 0007.
  - **Per-concept visual dashboard.** Read-side learning-curve view over confidence + retrieval history. Tracked as the *R-Later — Pedagogy dashboard* item under Later.
- **R5 — Worked-example → completion pairs.** For technical content with computation (ETA probability, FTA cut-sets, risk matrices), define a new task type: `worked-example` (study) → `completion` (fill blanked steps). Pi grades against a rubric prompt. Sweller & Cooper (1985); Renkl (2014, *example-based learning*). Bigger lift — needs a new task kind, completion-grading rubric, and frontend rendering.
- **R6 — Interleaving by default in plan structure.** `course-study-path` skill rule: after every 3–4 new-content tasks, insert a `revisit` task that surfaces an earlier phase's content via retrieval (couples with R1). Rohrer & Taylor (2007); Carvalho & Goldstone (2014). Skill-level change once R1 ships.
- **R7 — Interest-log activation (full version).** Today shipped a lightweight prompt-only surfacing rule (Rule 6 / Q6). Full version: time-based queue (`interests` table with `surfaced_at`, `closed`, `pursued`), weekly resurfacing of oldest entries with explicit close-or-pursue action, decay if ignored. Berlyne (1960, curiosity as the engine of self-directed learning).
- **R8 — Bloom-level enforcement at phase boundaries.** Spec'd 2026-06-08 (see `specs/queue/2026-06-08-bloom-enforcement.md`). Adds `bloom_level` field to Task + phase-completion gate requiring analyze/evaluate/create coverage. Waiting for overnight pipeline pickup.

### Practice-testing gap (grill-with-docs, 2026-06-02)

Current retrieval relies on **self-reported confidence** (`log_confidence`: 0.0–1.0), which is notoriously miscalibrated — learners routinely overestimate what they know (Dunlosky et al. 2013; Koriat 1997; Kruger & Dunning 1999). The SM-2 scheduler feeds on this uncalibrated signal, which means it may space items too far apart. The practice-testing items below replace confidence with **actual retrieval performance** and add calibration scaffolds.

The structural piece below — R9, real practice testing — is still open and is the biggest remaining gap. (R10 pre-testing, R11 free recall, and R12 elaborative interrogation shipped 2026-06-04…06 as prompt-level pedagogy rules in `agent/sandbox.go` — they're agent-instructed, not structurally enforced; see CHANGELOG.)

- **R9 — Practice testing (the biggest gap).** Agent generates a question from a Knowledge Component → learner types an answer → agent grades it (semantic comparison) → score feeds SM-2 schedule. Replaces self-reported `log_confidence` with actual performance as the retrieval scheduling signal. Roediger & Karpicke (2006, testing effect: retrieval itself strengthens memory). Depends on KC entity + retrieval loop + SM-2 (all shipped). Touches: LLM question generation, answer grading prompt, new `retrieval_probe` table or extending `confidence_log` with `grade` + `correct` columns.
- **R13 — Metacognitive calibration.** After a test question (R9), show the learner their predicted confidence alongside their actual score. Over time this improves calibration — the learner gets better at knowing what they don't know (Koriat 1997). Depends on R9. Needs: confidence prediction capture before answering, comparison UI in retrieval results.

## Later

Bigger things, not blocking, would need their own design pass.

- **Declared Task → resource association.** Phase 3 ships *learned* resources: a task auto-opens reading from its Session's `last_pdf_id`, which only exists after the first manual open (see the Phase-3 data-model decision / ADR). The richer version declares the resource up front so a never-opened task knows its PDF and auto-opens on first entry — needs new schema (task→PDF link, in plan JSON or a join) *and* an authoring surface to set it. Belongs with the **Authoring** flow (plan/course creation), not the IA rebuild — design it there.

- **Agent-emitted HTML snippets / interactive templates.** Let the LLM return inline UI fragments — flashcards, quizzes, diagrams, expandable comparison tables, code playgrounds — alongside prose. Needs a sanctioned subset of HTML (sanitized; restricted attributes; no scripts), a new content type in the SSE stream, and a renderer that can hydrate `data-action` hooks into the existing event delegator. Touches model trust boundary: prompt-inject defenses required.
- **Agent-generated files & downloads (especially PDFs).** Tool that lets the agent build a file (PDF summary, LaTeX export, JSON plan dump, flashcard deck) and surface it to the user as a download link in the chat. Server-side generation (likely `gofpdf` or `chromedp` headless render); files saved to a sandboxed dir under `data/agent-out/`; one-time signed URLs to avoid leaking. Decide auth/cleanup policy.
- **Memory system — brainstorm needed.** Today the agent has zero accumulated taste between sessions; every chat starts amnesiac. Decide the shape: per-user feedback memories (style/density/voice rules), conversation-summary memory (what was discussed last week), semantic memory of artifacts produced, project memory (what's in flight), or some union. Likely depends on the app↔Claude-runtime decision (replace vs complement). Brainstorm before designing storage, retrieval, or write-paths.
- **Knowledge base — the next major milestone (grill/brainstorm needed before any build).** Flagged 2026-06-17 as the intended next big direction. **The substrate is unusually ready but disconnected:** learner-authored atoms (`knowledge_components`, [ADR 0007](docs/adr/0007-knowledge-component-as-atomic-note.md)), a ~19k-chunk RAG corpus (`corpus_chunks`), the SM-2 retrieval schedule (`retrieval_queue`/`confidence_log`), `agent_memory`, and fleeting/formal notes — four separate piles. Today's corpus is only the maintainer-converted course markdowns; the *real* knowledge lives in formal `.tex` notes, fleeting `.md` notes, raw PDFs, and the agent's own past answers, none of which the corpus contains.
  - **The structural gap that separates "study tutor that takes notes" from a knowledge base: atoms are siloed.** Each `knowledge_component` links *down* to its origin (`source_task_id` / `source_session_id`) but never *sideways* to other atoms, and isn't linked to the corpus chunk it came from. That sideways-link primitive is the missing spine of a Zettelkasten-style *base* (vs. a pile).
  - **Convergence signal:** the *same* atom-linking primitive is what the deferred KC-arc *Link graph + multi-source provenance* follow-on needs AND what interleaving-v2 (confusable-family grouping, Rule 15 shipped 2026-06-17) needs — strong sign it's the right next foundation to lay.
  - **Open questions for the grill:** is the KB (a) a navigable graph of linked atoms (Obsidian-like), (b) an agent-queryable knowledge layer where the tutor reasons over *your* accumulated atoms, not just the corpus, (c) cross-course synthesis toward the "unified theoretical understanding across courses" goal (`memory/study-context.md`), and/or (d) a feeder into thesis work? Does it auto-ingest the user's actual work tree? What's the indexing primitive? How are fleeting-vs-formal notes weighted? Per-user KBs?
  - **Tension to weigh:** `memory/stability-plan.md` flags the app as architecturally fragile (28 issues, only partly remediated) — a milestone this size competes with hardening. Decide deliberately, don't drift.
  - **Grill before any indexer/schema work.** (Supersedes the earlier narrow "auto-ingest the corpus" framing; relates to the KC-arc *Link graph + multi-source provenance* item above.)
- **Coding surface — clarify shape, then design.** User asked for "some system to add coding" — could mean (a) the agent can write and execute code in a sandbox (DSA practice, runnable examples, scratch experiments), (b) a coding-practice mode for study sessions (drills, problem flow), or (c) both. Resolve scope before designing.
- **Agent-administered test questions / active recall.** Tool that generates and grades test questions from the corpus + plan progress. Connects to `study-methods/active-recall.md` and `study-methods/spaced-repetition.md`. Needs: question generation (LLM call), answer comparison (semantic, not exact), score persistence, scheduling (which question to ask next, when). Likely couples with the HTML-snippet item above so questions render as proper UI, not free-text. Big enough to want its own design doc before any code.
- **"Fast study" mode.** Undefined — placeholder for a low-friction path to short, lookup-style study moments (no orientation, no full session lifecycle?). Brainstorm before prioritising; capture the trigger first, then the shape.
- **Review the app ↔ Claw agent relationship.** Revisit how `claw-study` (this app) and Claw (the Telegram bot) divide responsibilities. Tier A `claw-study-read` skill is shipped; Tiers B/C/D from the original plan are speculative. Question is whether the Tier model is still right or whether the boundary should be redrawn. Output is likely an ADR (or one that supersedes Tier B/C/D below).
- **Tier B `claw-study-notes` skill.** Mutating skill for Claw — fleeting notes, plan toggles, memory edits.
- **Tier C `claw-study-api` skill.** HTTP API client for things the filesystem doesn't expose well (RAG search, chat).
- **Tier D `claw-study-deploy` skill.** Build / scp / systemctl / git ops on the repo.
- **R-Later — Pedagogy dashboard.** Once R4 (confidence trajectories) ships, surface a per-concept learning-curve view in the UI: confidence vs. time, retrieval success rate, items overdue for review. Backs the Bjork "desirable difficulties" framing (Bjork & Bjork 2011) — makes the fluency illusion visible.
- **R-Later — Term-budget self-enforcement.** Rule 8 is currently advisory (Pi may still violate it). A stronger version has Pi self-count new terms per turn and refuse to advance until the user demonstrates the previous batch. Sweller (1988) but instrumented. Lower ROI than R1–R4; nice-to-have polish.
- **R-Later — Deliberate-practice tracks.** When a concept's retrieval confidence stays low across multiple probes, branch the user into an isolated practice loop with targeted exercises and immediate feedback. Ericsson (1993). Depends on R1+R4 being in place.

## Won't do

Decisions deliberately ruled out. Listed so they're not relitigated by accident.

- **Service / repository layer split.** See [ADR 0002](docs/adr/0002-no-service-repository-layer.md).
- **Docker + Compose for the deploy.** See [ADR 0003](docs/adr/0003-no-docker-portability-first.md).
- **Stack rewrite (Go → anything else).** See [ADR 0001](docs/adr/0001-stay-with-go.md).
- **Frontend framework (HTMX / React / Svelte).** See [ADR 0004](docs/adr/0004-vanilla-js-frontend.md).
- **PR-based workflow.** See [ADR 0005](docs/adr/0005-push-to-main-no-prs.md).
